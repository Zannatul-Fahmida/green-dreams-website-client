import { Typography } from '@mui/material';
import React from 'react';

const DashboardHome = () => {
    return (
        <div>
            <Typography variant="h3" sx={{color: 'success.main'}}>This is Dashboard</Typography>
        </div>
    );
};

export default DashboardHome;